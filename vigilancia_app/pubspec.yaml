name: vigilancia_app
description: "Sistema de vigilancia y monitoreo comunitario - Sentinel Surveillance"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # ✅ CORREGIDO: rango válido de SDK

dependencies:
  flutter:
    sdk: flutter

  # ── Red y API ─────────────────────────────────────────────────
  http: ^1.2.0

  # ── Sesión / almacenamiento local ─────────────────────────────
  shared_preferences: ^2.2.3

  # ── Íconos iOS ────────────────────────────────────────────────
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
